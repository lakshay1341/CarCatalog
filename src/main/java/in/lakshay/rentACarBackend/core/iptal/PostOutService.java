package in.lakshay.rentACarBackend.core.iptal;

// interface for payment processing services
// looks like this is deprecated/not used anymore
// "iptal" means "cancel" in Turkish btw
public interface PostOutService {

    // commented out payment method - prob replaced by something else
    // keeping for reference but should prob delete this whole file
    //  public boolean payment(String cardNumber, String cardOwner, String cardCvv, String cardExpirationDate, double totalPrice);

    // todo: remove this file if not needed anymore
    // or implement properly if we still need it
}
